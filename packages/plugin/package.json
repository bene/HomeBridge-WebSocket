{
  "name": "@homebridge-ws/homebridge-ws-plugin",
  "displayName": "HomeBridge WebSocket",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "files": [
    "./dist"
  ],
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "npm run clean && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@homebridge-ws/types": "*",
    "@types/node": "^18.11.17",
    "homebridge": "^1.6.0",
    "typescript": "^4.9.4"
  }
}
